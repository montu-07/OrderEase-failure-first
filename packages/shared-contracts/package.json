{
  "name": "@orderease/shared-contracts",
  "version": "1.0.0",
  "description": "Shared contracts - DTOs, types, enums, interfaces, constants, and pure domain logic",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.7.2"
  }
}
